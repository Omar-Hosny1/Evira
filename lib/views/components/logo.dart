import 'package:flutter/material.dart';

class Logo extends StatelessWidget {
  const Logo({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('EVIRA',
    
    textAlign: TextAlign.center,style: TextStyle(letterSpacing: 10, fontSize: 26),);
  }
}